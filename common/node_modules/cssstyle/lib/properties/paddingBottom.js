'use strict';

var padding = require('cssstyle/lib/properties/padding.js');
var parsers = require('cssstyle/lib/parsers.js');

module.exports.definition = {
  set: parsers.subImplicitSetter('padding', 'bottom', padding.isValid, padding.parser),
  get: function() {
    return this.getPropertyValue('padding-bottom');
  },
  enumerable: true,
  configurable: true,
};
